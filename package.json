{
  "name": "nigel-frontend",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "mobile",
    "web"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}